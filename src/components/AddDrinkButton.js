import React from "react";
import { Button } from "@mui/material";
export default function AddDrinkButton() {
    return (
        <>
            <Button 
                variant="contained"
            >
                Add a new drink
            </Button>
        </>
        )
}